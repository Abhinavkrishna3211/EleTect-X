# MPU — QRB2210 (event cognition, Debian/Python)

Vision detector (Adreno/OpenCL), log-odds fusion, contextual-bandit deterrence, SQLite experience, Bridge RPC, LoRa uplink. On-device vision model in `models/`.
